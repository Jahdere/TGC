src/game/Unit.cpp View file @ 8f360b1
 @@ -6927,7 +6927,7 @@ void Unit::SetInCombatState(bool PvP, Unit* enemy)
      return;
  
    if (PvP)
-     m_CombatTimer = 5000;
+     m_CombatTimer = 5500;
  
    bool creatureNotInCombat = GetTypeId() == TYPEID_UNIT && !HasFlag(UNIT_FIELD_FLAGS, UNIT_FLAG_IN_COMBAT);